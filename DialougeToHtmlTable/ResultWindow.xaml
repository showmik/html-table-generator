<Window x:Class="DialougeToHtmlTable.ResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ResultWindow"
        Height="450"
        Width="800" MinWidth="400" MinHeight="300">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>

            <Grid Background="WhiteSmoke">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <StackPanel
                    Grid.Column="0"
                    Orientation="Horizontal">
                    <Button x:Name="ResultCopyButton" Content="Copy"
                        HorizontalAlignment="Left"
                        Width="60"
                        Margin="0,0,0,0"
                        Background="LightYellow"
                        BorderBrush="Yellow"
                        Click="ResultCopyButton_Click" />

                    <Button x:Name="ResultClearButton" Content="Clear"
                        HorizontalAlignment="Left"
                        Width="60"
                        Margin="0,0,0,0"
                        Background="LightPink"
                        BorderBrush="Pink"
                        Click="ResultClearButton_Click" />
                </StackPanel>
            </Grid>

            <ScrollViewer Grid.Row="1">
                <RichTextBox
                         x:Name="ResultTextBox"
                         Margin="0,0,0,0"
                    BorderBrush="Lavender"
                         SelectionBrush="DodgerBlue" />
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>